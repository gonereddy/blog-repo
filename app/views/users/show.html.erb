<div>
<h4>Approve the Users</h4>
<%= form_tag permissions_users_path do %>
<br><div class="form-group form-group-sm">
        <div class="row">
            <div class="col-xs-2" style="text-align:center;">Selct Users:
            </div>
            
              <div class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Selct Users: <i class="fa fa-chevron-down"></i></a>
                        <div class="dropdown-menu" style="margin-left:150px">
                          <%= select_tag  :status, options_for_select((User.
                            where(status: false).map {|r| [r.email,r.id] }) ), :multiple => true, class: 'form-control' %>
                        </div>
                    </div>
        </div>
   </div><br>
   <div class="form-group" style="padding-top:100px">
        <div class="col-sm-offset-2 col-sm-10" >
          <button type="submit" class="btn btn-default" style="margin:-20px 20px 20px 20px"><%= submit_tag "Approve" %></button>
        </div>
   </div>
   <% end %>
   </div><br>
   <script type="text/javascript">
    $('.dropdown-menu').click(function(e) {
        e.stopPropagation();
    });
</script>

   <%#= select_tag  :status, options_for_select((User.
              where(status: false).map {|r| [r.email,r.id] }) ),:prompt => "Select Users" , :multiple => true %>